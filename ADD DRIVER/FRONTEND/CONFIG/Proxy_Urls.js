const PORT = 4001;
const SERVER = `http://localhost:${PORT}`;
const API_FETCH_URL = `${SERVER}/api`;
const ORIGIN_URL = "http://localhost:1234";

module.exports = {
  API_FETCH_URL,
  ORIGIN_URL,
  SERVER,
  PORT,
};
